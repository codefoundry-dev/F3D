{
  "name": "@forethread/api-client",
  "version": "0.0.0",
  "private": true,
  "description": "Typed HTTP client for all Forethread API endpoints",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts --max-warnings 0"
  },
  "dependencies": {
    "@forethread/shared-types": "workspace:*",
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "@forethread/config": "workspace:*",
    "@forethread/eslint-config": "workspace:*",
    "typescript": "^5.5.4"
  }
}
