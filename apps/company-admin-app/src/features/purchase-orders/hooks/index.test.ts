// This file is a comment-only barrel — verify it loads without error
import './index';

describe('purchase-orders/hooks/index', () => {
  it('loads the module', () => {
    expect(true).toBe(true);
  });
});
