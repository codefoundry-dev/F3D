---
name: rfq-dashboard-vendor-us206
description: US 2.06 vendor RFQ dashboard Figma-fidelity pass — aligned vendor RfqListPage + preview panel to the buyer reference, reworked VENDOR_COLUMNS/VENDOR_QUICK_FILTERS
metadata:
  type: project
---

Vendor counterpart of [[rfq-dashboard-us206]] (buyer PO+CA). Figma section `2934:12122` "US 2.06 RFQ dasboard (Vendor)" in CFA6k0XCvImOmWXbBgdWYZ. Fidelity pass on pre-existing files (not greenfield); reference = the finished buyer `apps/web/src/features/rfqs/buyer/pages/RfqListPage.tsx`. Done 2026-06-18, UNCOMMITTED on main, NO backend changes.

**Vendor page reworked to mirror buyer** (`apps/web/src/features/rfqs/vendor/pages/RfqListPage.tsx`): removed the Create-new button (vendors can't create RFQs) so toolbar row-1 left slot is empty + controls (View/Save/Export/Settings) right-justified (`justify-end`); added page title via `usePageTitleStore` (own the app-shell header on `/rfqs`, clear on unmount); added `s.loadViews()` on mount; swapped inline dropdowns → shared `ViewSelectorDropdown`/`ExportDropdownButton`/`GroupByButton`/`ToolbarSearchToggle` + `useDropdown` (all from `@forethread/ui-components`); loading skeleton-rows → `<Spinner size="md">`; preview panel `fixed`-to-viewport → buyer-style `absolute right-8` anchored to clicked-row Y (`previewY` state). Vendor uses the SHARED store `createRfqTableStore` from `@forethread/rfq-shared` (buyer uses its own local store) — shared store already has loadViews/setShowCreateView etc.

**RECONCILIATION — quick filters**: Figma frames disagreed (main-dashboard.png had 5 incl. "Replenishment"/"Received"; filters-open.png + grouped-status.png had 4). Chose the **4-set** (dominant, 2-of-3 frames): My RFQs / Open RFQs / Approved for a vendor / Closed RFQs. `VENDOR_QUICK_FILTERS` (packages/rfq-shared/src/constants/filters.ts) = `['myRfqs','openRfqs','approvedForMe','closedRfqs']` (was `['myRfqs','incoming','approvedForMe','closedRfqs']`). Renamed `incoming`→`openRfqs` (backend `applyQuickFilter` handles both identically → status OPEN). KEPT key `approvedForMe` (backend already scopes APPROVED quotes to the current vendor under that key) but changed its i18n label to "Approved for a vendor". All 4 keys are backed by real backend logic + valid in the shared zod `rfq.schema` enum.

**RECONCILIATION — columns**: treated the dashboard **table header** (grouped-status.png) as canonical (11 cols). `VENDOR_COLUMNS` order = rfqId, projectName, contractorCompany, contractorName, rfqStatus, resDeadline, pickUp, deliveryLocation, pickUpLocation, lineItems, totalRespondedQuotes (was a different 8). **Data gaps (no vendor-only DTO fields on `RfqListItem`)**: `contractorCompany` + `contractorName` BOTH map to `field:'createdBy'` (DTO has one contractor identity, no org/person split); `totalRespondedQuotes` maps to `field:'recQuotes'` (received quotes — closest real metric, no "responded quotes" count exists). All keys already exist in the export `COLUMN_MAP` (rfq-export.service) so export passes through with no backend edit. Table-mgmt modal uses long `columnLabels.*` (added contractorCompany/contractorName/totalRespondedQuotes); short table headers use `columns.*` (added same 3). Keep keys aligned across VENDOR_COLUMNS + columns.* + columnLabels.* + export columns param.

**Shared constants are SAFE to edit for RFQ**: the PO vendor page imports its `VENDOR_COLUMNS`/`VENDOR_QUICK_FILTERS` from `@forethread/po-shared` (different package), NOT rfq-shared — so changing rfq-shared vendor constants only touches the RFQ vendor page.

**Preview panel** (`RfqDetailPanel.tsx`): title `rfq.projectName` → `rfq.rfqNumber ?? rfq.id` (Figma heading "RFQ-2024-008"); Response btn icon `LetterIcon` → `paper-plane.svg` (copy stays "Response"/"Edit Response"). **Panel body** (`VendorRfqDetailsTab.tsx` panel layout): section title `detailFields.rfqDetails`→`detailFields.basicInformation`, ADDED Project ID field (grid-cols-2 with RFQ ID) — Figma Basic Information shows RFQ ID + Project ID side-by-side. Page layout (`layout="page"`) UNCHANGED (still uses rfqDetails title).

**Tests**: NEW `RfqListPage.test.tsx` (9 tests — page-title wiring, no Create btn, shared toolbar present, spinner, 4 quick filters, column data, empty state). The list-page mock must export `VENDOR_COLUMNS` from the `@forethread/rfq-shared` mock (the page imports `../constants` which re-exports it) or it crashes at import. Updated `RfqDetailPanel.test.tsx` (title assertion projectName→rfqNumber; mock already had paper-plane.svg mocked) + `VendorRfqDetailsTab.test.tsx` (panel title→basicInformation, added projectId). Verified: 27 vendor primary tests, 188 all-vendor-rfqs, 293 buyer-rfqs all pass; web typecheck clean; prettier clean (RfqListPage.tsx + RfqDetailPanel.tsx needed --write). Harness `.tmp/figma-vendor-rfq/shot_vendor_rfq.mjs` (7 shots, role=VENDOR, vendor RfqDetail w/ empty quoteResponses) — rendered output matched all reference PNGs (table cols+order, 4 chips, grouped-by-status, filters panel, export dd, table-mgmt long labels, panel rfqNumber+paper-plane+Project ID).

**OUT-OF-SCOPE flagged (not changed)**: vendor full RFQ detail page (`RfqDetailPage.tsx`, node 3171:51395) + response flow — built under US 3.x, left alone per scope.

See [[rfq-dashboard-us206]], [[figma-fidelity-workflow]], [[forethread-web-screenshot-harness]], [[vendor-finance-dashboard-figma-fidelity]] (paper-plane precedent).
